name: anywhere_music_player
description: A self-hosted music streaming app for Web and Android TV
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: any

  # State Management
  provider: any

  # HTTP Client
  http: any

  # Audio Player
  just_audio: any
  just_audio_windows: any

  # Windows System Media Transport Controls (taskbar media controls)
  smtc_windows: any

  # Local Storage (JWT tokens)
  shared_preferences: any

  # JSON Serialization
  json_annotation: any

  # Environment Configuration
  flutter_dotenv: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any
  build_runner: any
  json_serializable: any

flutter:
  uses-material-design: true

  assets:
    - .env
